# Diz para o Spring: "Leia também as configurações do arquivo -local"
spring.profiles.active=local

# Configurações gerais que não são segredo
spring.application.name=ergane-api

# Opcional: mostrar as requisições do Mongo no log para facilitar o debug
logging.level.org.springframework.data.mongodb.core.MongoTemplate=DEBUG